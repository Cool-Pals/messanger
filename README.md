# messenger

## Build the ui

```bash
cd ui && npm run build
```

## Start the app

To start the app from the repo root folder:

```
    docker compose up -d
```

UI is reachable by the localhost/msg


